{
  "name": "supernoba-order-router",
  "version": "1.0.0",
  "description": "Order Router with Kinesis and DynamoDB",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "deploy": "aws lambda update-function-code --function-name Supernoba-order-router --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-kinesis": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@supabase/supabase-js": "^2.89.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
